FROM golang:1.24-alpine

ENV GONOSUMDB=*
ENV GOFLAGS=-mod=mod
ENV CGO_ENABLED=0

RUN apk add --no-cache git

WORKDIR /app

EXPOSE 8081

# go run downloads deps automatically on first start (source mounted via volume)
CMD ["go", "run", "./cmd/server/main.go"]
